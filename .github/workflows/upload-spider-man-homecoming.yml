name: Spider man home coming upload
on:
  workflow_dispatch:
jobs:
  copy:
    runs-on: ubuntu-latest
    timeout-minutes: 350
    steps:
      - name: Install rclone
        run: curl https://rclone.org/install.sh | sudo bash

      - name: Write rclone config
        env:
          GDRIVE_SECRET: ${{ secrets.GDRIVE_SECRET }}
          R2_ACCESS_KEY: ${{ secrets.R2_ACCESS_KEY }}
          R2_SECRET_KEY: ${{ secrets.R2_SECRET_KEY }}
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
        run: |
          mkdir -p ~/.config/rclone
          cat > ~/.config/rclone/rclone.conf <<EOF
          [gdrive]
          type = drive
          scope = drive.readonly
          token = ${GDRIVE_SECRET}

          [r2]
          type = s3
          provider = Cloudflare
          access_key_id = ${R2_ACCESS_KEY}
          secret_access_key = ${R2_SECRET_KEY}
          endpoint = ${R2_ENDPOINT}
          acl = private
          EOF
          chmod 600 ~/.config/rclone/rclone.conf

      - name: Refresh token
        run: "rclone about gdrive:"

      - name: Download file via Drive API
        env:
          FILE_ID: 1H6yP8ItXHjAT69uL8CTAlvi90sPx2399
          OUTPUT: spider-man-homecoming.mp4
        run: |
          ACCESS_TOKEN=$(python3 -c "
          import json, configparser, os
          c = configparser.ConfigParser()
          c.read(os.path.expanduser('~/.config/rclone/rclone.conf'))
          print(json.loads(c['gdrive']['token'])['access_token'])
          ")

          curl -L --fail --progress-bar \
            -H "Authorization: Bearer $ACCESS_TOKEN" \
            "https://www.googleapis.com/drive/v3/files/${FILE_ID}?alt=media" \
            -o "$OUTPUT"

          ls -lh "$OUTPUT"

      - name: Upload to R2
        run: |
          rclone copyto "spider-man-homecoming.mp4" "r2:nostaligafun-files/games/games-accualy/APPS/MovieApp_Data/files/movies/spider-man-homecoming.mp4" \
            --s3-no-check-bucket \
            --progress \
            --stats 30s \
            --retries 999 \
            --low-level-retries 999
